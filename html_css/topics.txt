css is cascading stylesheet
Css can be applied in 3 ways
Responsive web design:

internal->can be defined in head tag
external->can be defined in head tag
inline->can be deinfed in the element itself

In 3 ways can apply css style to the elements;
usigng tag name.
using id(should be unique,can be access using #)
using class(it can be duplicate,can be access using .)
measure:px,%
color(hex code or rgb value)
    text color for the element
background-color
    backgroud-color for the elemen
font-size
    text size for the element
width
    widht of the elementz
height
    height of the element
overflow
    when content is overflow morethan mentioned height or width.
        hidden->hide teh overflow content
        auto->add the scroll bar
                if contnet overflowas add the scroll bar or no need to scroll
text-align:
    center,left,right
border
    add border color and stype aroud the element
    1.sizeofborder
    2.border_type
    3.colorBorder

border-radius
    radius around the elements
text-decoration
    to add underline or remove underline


display
    hide or show the element
    none->to hide the element
    block->to show the element

opacity->transparency
0.1.0.2...0.9,1
float
    move the element to right or left
margin->create a space around the element without background color;
margin:10px;//will apply for all the sides.
margin:10px 5px;
margin:10px 5px 10px;
margin:10px 5px 10px 5px;
 

margin-top
margin-bottom
margin-right
margin-left


padding->create a space around the element with background color;
padding:10px;//will apply for all the sides.
padding:10px 5px;
padding:10px 5px 10px;
padding:10px 5px 10px 5px
padding-top
padding-bottom
padding-right
padding-left



position->fixed,relative,absolute

position->fixed;
top|bottom:measure
right|left

fixed->always controlled by browser.
absolute
    1.->always controlled by browser.
    2.it scroll happens..will also move.
    3.if any parent element has position.then it will controlled by parent.


position->relative,absolute

parent and child elemnets with css
    connection between overlow and float 
        Parent element will not have height when all the children elements becomes float..
        :hover
        :focus